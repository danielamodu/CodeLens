services:
  - type: web
    name: codelens
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview -- --port $PORT --host
    envVars:
      - key: NODE_VERSION
        value: 20
